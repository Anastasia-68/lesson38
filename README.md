# Описание проекта
Учебный React-проект, созданный с помощью **Vite**.  
В проекте реализован простой компонент **кнопки и/или поля ввода**, который принимает `props` и обрабатывает события (`onClick`, `onChange`).

## 🛠️ Используемые технологии
- React
- Vite
- JavaScript (JSX)
- Node.js
- npm
 
 # Установка и запуск проекта
  `bash
   npm run dev


